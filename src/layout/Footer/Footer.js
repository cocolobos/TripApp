import React from 'react';
import style from './footer.module.css';

function Footer () {
    return (
        <div className={style.footerSide}>
            <p className={style.footerP}>Siempre recuerda que viajar es tu mejor inversion || Nunca se vuelve siendo el mismo</p>
        </div>
    )
}

export default Footer;